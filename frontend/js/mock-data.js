/**
 * Mock data removed — app runs in live mode only.
 * Empty fallbacks prevent crashes if the script tag still exists.
 */
window.MOCK_SHOPS = [];
window.MOCK_AFFILIATES = [];
window.MOCK_CAMPAIGNS = [];
window.MOCK_TREND = { labels: [], gmv: [], orders: [] };
